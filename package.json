{
  "name": "wynncraft-bot",
  "version": "2.0.0",
  "description": "Wynncraft Discord Bot",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "node deploy-commands.js",
    "test": "echo \"テストは後で追加します\" && exit 0",
    "lint": "echo \"Lintは後で追加します\" && exit 0",
    "version:patch": "npm version patch && git push && git push --tags",
    "version:minor": "npm version minor && git push && git push --tags",
    "version:major": "npm version major && git push && git push --tags"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "cheerio": "^1.1.0",
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.1",
    "node-cron": "^4.2.1",
    "puppeteer": "^24.12.1",
    "puppeteer-core": "^24.12.1"
  },
  "engines": {
    "node": ">=16.9.0"
  }
}
